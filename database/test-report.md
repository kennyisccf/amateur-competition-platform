# 乐赛 (LeSai) 数据库测试报告

## 1. 测试环境
- **数据库**: MySQL 8.0.46
- **管理工具**: VS Code Database Client
- **测试日期**: 2026-05-18

## 2. 数据库结构验证 (DDL Test)
- [x] **表创建测试**: 所有 6 张核心表均能通过脚本一键生成，无语法错误。
- [x] **外键约束测试**: 尝试删除存在关联数据的 `user` 记录，系统触发 `Foreign Key Constraint` 报错，符合预期。
- [x] **字段约束测试**: `username` 重复插入测试，触发 `Duplicate entry` 报错，符合预期。

## 3. 业务逻辑测试 (DML Test)
| 测试场景 | 执行语句描述 | 预期结果 | 实际结果 |
| :--- | :--- | :--- | :--- |
| **用户注册** | 向 `user` 表插入新玩家数据 | 成功生成 ID，默认积分为 0 | 通过 |
| **赛事发布** | 插入 `competition` 数据，状态为待审 | 成功关联主办方 ID | 通过 |
| **报名流程** | 插入 `registration` 数据 | 防止重复报名索引生效 | 通过 |
| **通知发布** | 向 `notice` 写入赛程变更消息 | 成功关联对应赛事 ID | 通过 |
| **积分流水** | 模拟加分并记录到 `point_history` | 流水记录清晰展示变动原因 | 通过 |

## 4. 结论
数据库 V2.1 版本表结构设计合理，约束严密，预置数据覆盖了企划书要求的所有核心角色和业务场景，具备进入后端开发阶段的条件。